#!/bin/sh
# debconf config script for pglcmd

set -e

# All code was moved to debian/pglcmd.postinst.
# TODO: Bring it back here, but make sure that
# - purge cleans debconf db
# - configuration works (http://bugs.debian.org/cgi-bin/bugreport.cgi?bug=352697)

# Source debconf library.
. /usr/share/debconf/confmodule

db_version 2.0

# This (deb)conf script is capable of backing up
db_capb backup

# dh_installdeb will replace this with shell code automatically
# generated by other debhelper scripts.

#DEBHELPER#

exit 0
